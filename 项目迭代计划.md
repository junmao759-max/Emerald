# Emerald 迭代规划

> 由 Trend Researcher 基于 2026 年真实数据调研产出（AI Agent 化、本地优先、双向链接、命令面板、悬浮预览、Git 集成等方向）。
> 技术栈前提：Electron + 纯原生 JS（无框架、无打包器、无 TypeScript），自研 md-parser / highlighter，数据持久化目前用 localStorage。

---

## 一、趋势洞察

1. **AI 从"问答"转向"Agent 执行"，但本地优先是硬需求** —— 2026 年笔记工具的 AI 已从"总结问答"演进为"能自己归档、建链、整理结构的 Agent"，同时 Obsidian + 插件、Xplorer、Voyager 等全部强调"本地优先 / BYO-Key（自带 API Key）/ 数据不出设备"。对 Emerald 最现实的是 **BYO-Key 本地 AI 助手**（DeepSeek / Ollama 本地模型），而非自建云端服务。

2. **双向链接 + 反链 + 图谱是知识管理品类的最强护城河** —— Obsidian 靠 `[[wikilink]]`、反链面板、图谱视图确立差异。Emerald 已自研 md-parser，**支持 `[[ ]]` 双向链接是顺理成章、复用度最高的下一步**。

3. **命令面板正变成"操作中枢"而非快捷键补充** —— Cursor 3.0 直接把命令面板升级为 "Agents 面板"；VS Code 的 Ctrl+Shift+P 统一所有命令。Emerald 已有 Ctrl+P 快速切换器，**把它扩展成完整的命令注册表 + 命令面板，是后续所有功能（AI、插件、批量操作）的统一入口**。

4. **悬浮预览（Peek/Hover）成为编辑器的标准交互** —— VS Code 的 `Alt+F12` Peek 定义、`Ctrl+Hover` 悬浮预览，都指向"不离开当前上下文就能预览"。Emerald 的自研 md-parser 和 highlighter 是**现成的预览引擎**，做悬浮预览边际成本极低。

5. **Git 集成和"本地优先 + 可移植 Markdown"是开发者用户的核心诉求** —— Xplorer（Tauri 开源文件管理器）把 Git 集成、内置终端、扩展市场作为卖点。Emerald 的中文开发者用户几乎必然用 Git，**树内脏文件标记 + 差异视图**是高价值、差异化明确的开发者向功能。

---

## 二、功能机会清单

> 价值/难度均基于当前技术栈评估。所有功能均无需引入 React/Vite/TS。

| # | 功能 | 价值 | 难度 | 依赖 | 状态 |
|---|------|------|------|------|------|
| 1 | 会话恢复：启动时还原上次工作区 + 打开的标签页 + 光标位置 | 高 | 低 | 无 | ✅ 已完成 |
| 2 | 命令注册表 + 完整命令面板（Ctrl+Shift+P） | 高 | 低 | 无（但它是 14、12 的基石） | ✅ 已完成 |
| 3 | 悬浮预览：Hover/快捷键浮层预览 Markdown + 代码高亮 | 高 | 低-中 | 复用 md-parser.js + highlighter.js | ✅ 已完成 |
| 4 | 最近文件 + 收藏/置顶 | 中 | 低 | 无 | ✅ 已完成 |
| 5 | 标签系统：文件/文件夹打标签，侧栏按标签过滤 | 高 | 中 | 需选持久化方案 | ✅ 已完成（`.emerald/index.json` 侧车） |
| 6 | 全文内容搜索：从文件名搜索升级为内容搜索 + 高亮命中行 | 高 | 中 | 升级现有主进程递归搜索 | ✅ 已完成 |
| 7 | 双向链接 wikilink + 反链面板 | 高 | 中-高 | 扩展自研 md-parser | ✅ 已完成 |
| 8 | Markdown 增强：TOC、表格、任务列表勾选、内嵌图片 | 中 | 中 | 扩展自研 md-parser | ✅ 已完成（内嵌图片相对路径按当前文件目录解析） |
| 9 | 图片 / PDF 预览 | 中 | 低-中 | 无 | ✅ 已完成（PDF 用 Chromium 内置查看器 + 系统打开兜底） |
| 10 | Git 集成：树内脏文件标记、单文件 diff、提交面板 | 高 | 中-高 | child_process 调 git | ⬜ 待办（P2） |
| 11 | 多面板分屏（VS Code 式 split） | 高 | 中-高 | 编辑器/标签布局小重构 | ⬜ 待办（P2） |
| 12 | AI 助手（BYO-Key）：DeepSeek / OpenAI / Ollama 本地模型 | 高 | 高 | safeStorage + preload IPC + 流式渲染 | ⬜ 待办（P2） |
| 13 | 知识图谱视图：wikilink 力导向布局 + 孤岛检测 | 中-高 | 高 | 依赖 #7 | ⬜ 待办（P3） |
| 14 | 插件 / 用户脚本系统 | 中 | 高 | 依赖 #2 | ⬜ 待办（P3） |
| 15 | 批量重命名（正则/序号） | 中 | 低-中 | 无 | ⬜ 待办（P3） |

**技术建议（贴合现有栈）：**
- 标签、收藏等元数据**不要继续塞 localStorage**（它当前只存"最近文件夹/主题"，够用，但元数据量大后应落到文件侧车，如工作区根目录 `.emerald/index.json`）——这是学习"结构化持久化"的好机会。
- AI 的 API Key **严禁写入 localStorage**（明文、无加密），应使用 Electron 的 `safeStorage` API 加密后再落盘——本身就是一次有价值的"安全编码"实践。

---

## 三、分阶段路线图

### P0 ——「从能用变好用」（低悬果实 + 打地基）
**主题**：用最小成本建立统一命令体系，复用已有 parser/highlighter 快速产出可见价值。
**功能**：#1 会话恢复、#2 命令面板、#3 悬浮预览、#4 最近文件 + 收藏。
**排序理由**：
- 全部无硬依赖、低难度，纯 JS 栈下快速落地，快速获得正反馈。
- 命令注册表是战略基石：把散落的右键菜单/快捷键/按钮统一成一个可搜索入口，后续 AI、插件、批量操作都挂在这个注册表上，避免后期返工。
- 悬浮预览复用度最高：md-parser 和 highlighter 已是现成引擎，一次投入同时服务编辑器、树、搜索结果三个场景，学习价值（抽象复用）也最高。

### P1 ——「走向知识管理」（对齐 Obsidian 核心差异）✅ 已完成
**主题**：从"文件管理器"跨入"知识管理工具"，抓住双向链接/标签/检索三大趋势。
**功能**：#5 标签系统、#6 全文内容搜索、#7 双向链接 + 反链面板、#8 Markdown 增强、#9 图片/PDF 预览。
**排序理由**：
- #6 全文搜索是 #5 标签系统之外的第二条"找到东西"的路径，二者互为补充，价值密度最高。
- #7 双向链接是这个阶段的"标志性功能"，直接对齐趋势第 2 条；技术上只需扩展自研 parser，不引入任何框架。
- #8/#9 是 #7 的自然配套（笔记里会有图片、表格、目录），放在同阶段一起完善 Markdown 体验。

### P2 ——「开发者向 + AI Agent」（本地优先）
**主题**：切入开发者工作流 + AI 趋势，保持"本地优先"哲学。
**功能**：#10 Git 集成、#11 多面板分屏、#12 AI 助手（BYO-Key）。
**排序理由**：
- 用户画像（中文个人开发者）对 Git 的诉求最硬，#10 与 #11 都服务"写代码/看代码"场景，一起做能共享"编辑器布局重构"的收益。
- #12 AI 助手是 2026 最大趋势，但必须放在 #2 命令面板之后（AI 动作作为命令挂进去）、且要在 P1 把持久化/安全基础打好；技术上需要 safeStorage + 流式渲染，难度最高，故放 P2 末尾。
- 明确不做云端 AI 服务：走 DeepSeek/Ollama 本地模型 + BYO-Key，守住"本地优先、数据不出设备"的立场。

### P3 ——「生态化与高阶可视化」
**主题**：从"工具"到"平台"，做高学习价值、高难度的进阶项。
**功能**：#13 知识图谱视图、#14 插件/用户脚本系统、#15 批量重命名。
**排序理由**：
- #13 依赖 #7 的反链数据，是知识管理叙事的"压轴可视化"，但力导向布局、Canvas 渲染在纯 JS 下难度高，故延后。
- #14 依赖 #2 的命令注册表，是"生态化"的方向，但纯 JS 无打包器的插件沙箱/隔离是真正的挑战，作为高阶学习目标而非必须交付。
- #15 独立低难度，作为 P3 的"轻松收尾"平衡阶段压力。

**明确不做 / 推迟**：
- **云同步**：与"本地优先 + 学习项目"定位冲突，且对单人开发者价值低，用"导出/备份工作区索引"代替即可。
- **React/Vite/TypeScript 重构**：无必要。自研 parser/highlighter 和 `<script src>` 拆分本身就是该项目最有价值的学习资产，重构会摧毁这些学习收益；除非未来明确要上插件市场级生态，否则保持现状。

---

## 四、建议的下一步（先做这 1-2 个）

> **进度打卡（2026-08）**：P0 全部（#1–#4）与 P1 全部（#5–#9）已完成。
> 下一步进入 P2「开发者向 + AI Agent」：**#10 Git 集成** 是 P2 里对中文开发者最硬的诉求，
> 且难度可控（child_process 调 git 即可，无需新依赖）；做完 #10 后接 #12 AI 助手
> （需 safeStorage 加密存 Key + 流式渲染，是最高难度的收尾项）。

1. **Git 集成（#10）** —— 树内脏文件标记 + 单文件 diff + 提交面板，`child_process` 调 git，
   与 #11 分屏共享"编辑器布局重构"的收益，是 P2 里性价比最高的一步。

2. **AI 助手（#12）** —— BYO-Key 本地优先（DeepSeek / Ollama），命令挂进 #2 命令面板，
   必须先用 `safeStorage` 解决 API Key 落盘加密，再谈流式渲染。

一句话理由：**P1 已收尾，下一步 #10 Git 打底、#12 AI 出彩，二者都守住"本地优先、数据不出设备"的立场。**

---

## 调研来源

- AI 笔记/第二大脑工具对比：frankx.ai、tana.inc、youngju.dev
- AI 笔记进入 Agent 时代（Lumina Note）：cloud.tencent.com
- 桌面 AI 文件管理器趋势（Manus / Voyager / Xplorer / AI-Cubby）：PCMag、everydev.ai、github.com/kimlimjustin/xplorer
- Obsidian 知识图谱 / 插件生态：till-freitag.com、github.com/enduserlab/canvas-renderer
- Obsidian 双向链接/标签/云同步：obsibrain.com、Obsidian 论坛 Wikilink Types
- Cursor 3.0 / 命令面板 / Agent 趋势：dargslan.com、dev.to
- VS Code Peek / Hover 预览：php.cn、cloud.tencent.cn
