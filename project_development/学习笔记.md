# 📚 Emerald 文件管理器 — 新增代码学习笔记

> 本文档对应本项目新增的文件管理器功能（启动界面、目录树、文件编辑保存）。
> 配套代码文件：`index.js`（主进程）、`preload.js`（安全桥）、`index.html`（渲染进程）。
> 启动方式：在项目目录执行 `npm start`。

---

## 一、先看全貌：三大进程各司其职

Electron 应用永远由三层组成，**每一层权限不同**：

```
┌─────────────────────────────────────────────────────────┐
│  主进程 index.js（有全部 Node 权限：fs、dialog、窗口）     │
│    ↑ ipcMain.on / ipcMain.handle                         │
├─────────────────────────────────────────────────────────┤
│  preload.js（安全桥，把主进程能力"翻译"给页面）             │
│    contextBridge.exposeInMainWorld('electronAPI', ...)   │
├─────────────────────────────────────────────────────────┤
│  渲染进程 index.html（页面，只有浏览器能力，碰不到磁盘）      │
│    window.electronAPI.xxx()                              │
└─────────────────────────────────────────────────────────┘
```

**为什么不能直接让页面读写文件？**

因为 `contextIsolation: true` 下页面是"隔离沙箱"，没有 Node 能力——这是安全设计，防止网页代码窃取磁盘上的文件。所有文件操作都必须在主进程做，页面只能"请求"主进程帮忙。

---

## 二、核心概念：两种 IPC 通信

理解全部新增代码的关键，就两条规则：

| 场景 | 通道 | 谁调用 | 有返回值吗 |
|---|---|---|---|
| 窗口控制（最小化/最大化/关闭） | `ipcRenderer.send` → `ipcMain.on` | 通知型 | ❌ 没有，点一下就完 |
| 文件操作（读/写/列目录） | `ipcRenderer.invoke` → `ipcMain.handle` | 请求/响应型 | ✅ 有，`await` 等结果 |

**选择依据：需不需要返回值。**

- 窗口控制不需要结果 → `send`
- 文件操作需要把内容拿回页面 → `invoke`

---

## 三、主进程 index.js — 新增 5 个处理器

所有文件系统代码都返回统一的 `{ ok, ... }` 信封——`ok: true` 成功，`ok: false` 时带 `error` 说明。页面统一判断 `res.ok` 即可，不用猜。

### ① 选文件夹 `dialog:selectFolder`

```js
const result = await dialog.showOpenDialog(mainWin, {
    title: '选择工作区文件夹',
    properties: ['openDirectory'],  // 关键：只允许选文件夹
})
if (result.canceled || result.filePaths.length === 0) {
    return { ok: false, canceled: true }  // 区分"取消"和"失败"
}
return { ok: true, path: result.filePaths[0] }
```

调起系统原生文件夹选择框，`openDirectory` 限定只能选文件夹。

### ② 列目录 `fs:readDir` —— 最重要的一个

```js
const entries = await fs.readdir(dirPath, { withFileTypes: true })
// withFileTypes:true 才能拿到每个条目是文件还是文件夹
const items = entries
    .filter((e) => !e.name.startsWith('.'))   // 隐藏 .git/.env 等
    .map((e) => ({
        name: e.name,
        path: path.join(dirPath, e.name),     // 拼出完整绝对路径
        isDir: e.isDirectory(),
        isFile: e.isFile(),
    }))
items.sort((a, b) => {
    if (a.isDir !== b.isDir) return a.isDir ? -1 : 1  // 文件夹永远排前面
    return a.name.localeCompare(b.name, 'zh')          // 中文友好排序
})
```

- `withFileTypes: true` 让每条目录项带类型信息，否则还得二次 `stat`
- 排序先分"文件夹在前"，同类再按名称比

### ③ 读文件 `fs:readFile`

```js
const content = await fs.readFile(filePath, 'utf-8')  // 按文本读，返回字符串
return { ok: true, content }
```

### ④ 写文件 `fs:writeFile`

```js
await fs.writeFile(filePath, content, 'utf-8')  // 覆盖写入
return { ok: true }
```

页面把编辑后的字符串传过来，覆盖写回磁盘。

### ⑤ 文件信息 `fs:stat`

```js
const s = await fs.stat(filePath)
return { ok: true, size: s.size, mtimeMs: s.mtimeMs }
```

返回 `size`（字节数）和 `mtimeMs`（修改时间毫秒时间戳），给编辑器头部展示"大小 · 修改时间"。

> **⚠️ 重点：每个 handler 都用 `try/catch` 包住，异常时返回 `{ ok: false, error: err.message }`。**
> 文件操作**一定会失败**（路径不存在、权限不足、文件被占用）。不包住异常，Promise 会挂掉，页面 `await` 直接抛错白屏。

---

## 四、preload.js — 一扇门，只放行白名单方法

```js
contextBridge.exposeInMainWorld('electronAPI', {
    selectFolder: () => ipcRenderer.invoke('dialog:selectFolder'),
    readDir: (dirPath) => ipcRenderer.invoke('fs:readDir', dirPath),
    readFile: (filePath) => ipcRenderer.invoke('fs:readFile', filePath),
    writeFile: (filePath, content) => ipcRenderer.invoke('fs:writeFile', filePath, content),
    getFileInfo: (filePath) => ipcRenderer.invoke('fs:stat', filePath),
})
```

页面只能通过 `window.electronAPI` 上的这 5 个方法碰文件——不能直接 `require('fs')`。**你能暴露什么，页面就能用什么；没暴露的，页面拿不到。** 这就是安全边界。

---

## 五、渲染进程 index.html — 核心逻辑（重点学习）

### 5.1 全局状态 `state` —— 整个应用的"记忆"

```js
const state = {
    rootPath: null,        // 当前工作区根目录
    expanded: new Set(),   // 已展开的文件夹路径集合
    currentFile: null,     // 当前打开的文件
}
```

- `expanded` 用 `Set` 存"展开过的文件夹路径"——判断"点一下是展开还是收起"的依据
- `currentFile` 是编辑器的心跳，存着文件内容

### 5.2 目录树：`renderItems` + `toggleFolder`

**`renderItems(items, container, depth)`** —— 纯 DOM 渲染函数：

- `depth` 控制缩进：`paddingLeft = depth * 16 + 8px`，越深越靠右
- 文件夹行 = 箭头 `❯` + 文件夹图标 + 名字；文件行 = 文件图标 + 名字
- **用 `textContent` 而不是 `innerHTML` 放文件名**——文件名可能是 `<script>` 恶意内容，`textContent` 把它当纯文本，天然防 XSS
- 文件夹行点击 → `toggleFolder`；文件行点击 → `selectFile`

**`toggleFolder`** —— 懒加载的核心：

```js
if (state.expanded.has(p)) {
    // 已展开 → 收起：清空子节点，移出集合
    children.innerHTML = ''
    caret.classList.remove('open')
} else {
    const res = await window.electronAPI.readDir(p)  // 真正读磁盘
    renderItems(res.items, children, depth + 1)      // 递归渲染下一层
    state.expanded.add(p)
    caret.classList.add('open')
}
```

**懒加载 = 只有展开时才去读磁盘**，不是启动时把整个目录树全读一遍。Obsidian / VS Code 都这么干，文件夹多时性能差距巨大。

### 5.3 打开文件：`selectFile` —— 分流逻辑

点击文件后，代码按文件类型兵分三路：

1. **图片**（`.png/.jpg/.gif`…）→ `showImage`，用 `file:///` 协议直接渲染
2. **非文本**（未知二进制）→ `showUntextable`，提示无法查看
3. **文本**（扩展名在白名单 `TEXT_EXTS` 里）→ 读内容进编辑器

```js
state.currentFile = {
    path: item.path,
    name: item.name,
    content: res.content,          // 当前编辑的内容（会变）
    originalContent: res.content,  // 磁盘上的原始版本（不变）
}
```

`originalContent` 是"未保存检测"的关键——**只要 `content !== originalContent` 就是未保存**。

### 5.4 编辑与保存：`isDirty` + `updateSaveStatus` + `saveFile`

```
用户打字 → textarea 的 input 事件 → state.currentFile.content 更新
        → updateSaveStatus() 判断 content 是否等于 originalContent
        → 不等就显示"● 未保存"，点亮保存按钮
```

`saveFile()` 写回磁盘后把 `originalContent` 更新为当前内容，重置"未保存"状态：

```js
f.originalContent = f.content  // 已保存，基线前移
```

这个"改一个变量就能恢复干净状态"的设计非常典型，值得记住。

### 5.5 工作区生命周期：`chooseFolder` → `loadRoot`

启动界面点"打开文件夹" → `chooseFolder`：

1. `selectFolder()` 拿路径
2. `loadRoot()` 读根目录、渲染目录树
3. `launchScreen.classList.add('hidden')` 隐藏启动界面，主界面自然露出来

> 没有路由跳转，靠显示/隐藏两个界面切换，简单直接。

### 5.6 防丢失的"保护罩"（Obsidian 风格完善）

三处 `window.confirm` 确认框，防止辛苦打的字被冲掉：

- **切文件前**：有未保存修改 → 问一声
- **刷新工作区前**：同上
- **关窗口前**：有未保存修改 → 问一声

### 5.7 快捷键与完善点

- **Ctrl+S / Cmd+S 保存**：`document.addEventListener('keydown', ...)` 全局监听，`e.preventDefault()` 阻止浏览器默认的"另存为"
- **启动界面自带最小化/关闭按钮**：启动界面是 `position: fixed` 全屏遮罩，会盖住原窗口控制栏，所以补了两个窗口按钮
- **侧栏操作 rail**：刷新（重读根目录）+ 全部收起（清空所有子节点，只留第一层）
- **顶部路径指示条**：显示当前工作区路径（Obsidian 顶栏风格）
- **编辑器头部**：文件名 + 大小 + 修改时间 + 保存状态 + 保存按钮

---

## 六、一次点击的完整数据流（串起来理解）

```
用户点击"打开文件夹"
   └─ window.electronAPI.selectFolder()
       └─ (IPC) → 主进程 dialog.showOpenDialog → 系统选择框
       └─ 返回 { ok:true, path:"D:\\..." }
       └─ loadRoot() → readDir → 渲染第一层目录树

用户点击文件夹行
   └─ toggleFolder → readDir(该文件夹路径) → renderItems 渲染子层

用户点击 .md 文件行
   └─ selectFile → readFile(路径) → 内容放进 state.currentFile
   └─ renderEditor → textarea 显示内容

用户打字、按 Ctrl+S
   └─ saveFile → writeFile(路径, 新内容) → originalContent 前移 → "已保存"
```

---

## 七、顺带修复 + 发现的问题

1. **顺手修了旧 bug**：关闭按钮原来调用 `closewindow()`（全小写），preload 暴露的是 `closeWindow`（大写 W），属性大小写敏感所以失效——现在统一成 `closeWindow`。
2. **自动化检查抓到新 bug**：`fileCatalogue` 只有 `class` 没有 `id`，脚本却用 `getElementById('fileCatalogue')` 取它，会返回 `null` 白屏——已补上 `id`。
3. **建议清理**：`package.json` 里 `dependencies` 的 `fs` 和 `path` 是**错误安装的 npm 假包**（`fs@0.0.1-security`）。`fs` 和 `path` 是 Node 内置模块，根本不需要 `npm install`。留着不仅没用，还容易误导新手。可以删掉。

---

## 八、测试清单

```bash
npm start
```

1. 启动界面出现 → 点"打开文件夹"选一个目录
2. 目录树渲染第一层，文件夹在前
3. 点文件夹 → 展开子目录（箭头旋转 90°）；再点 → 收起
4. 点文本文件（如 `package.json`）→ 右侧显示内容 + 头部显示大小和时间
5. 修改内容 → 状态变"● 未保存"，保存按钮亮起；Ctrl+S 保存后变"已保存"
6. 改完不保存直接点关闭 → 弹出确认框
7. 点图片文件 → 预览；点 `.exe` 等二进制 → 提示无法查看
8. 侧栏两个按钮：刷新 / 全部收起

---

## 九、第二轮：8 大功能全面上线

第一轮的笔记讲了「进程分工 + 两种 IPC + 目录树 + 编辑保存」。这一轮把管理器升级成完整产品，一次补上 8 个功能。渲染层代码全部收进 `renderer/` 三个文件，用经典 `<script src>` 按顺序加载：

```html
<script src="./renderer/md-parser.js"></script>     <!-- ① Markdown → HTML -->
<script src="./renderer/highlighter.js"></script>    <!-- ② 代码 → 彩色 token -->
<script src="./renderer/app.js"></script>            <!-- ③ 主逻辑（依赖①②） -->
```

顺序不能乱：`app.js` 在运行时调用 `MarkdownParser` 和 `SyntaxHighlighter`，它们必须先被加载。

---

### 9.1 全局状态升级：从「一个文件」到「一组标签页」

第一轮 `state.currentFile` 是一个普通对象，直接存当前文件。这一轮改成**多标签**，核心设计是「当前文件 = 从 tabs 里算出来的」而不是「单独存一份」：

```js
const state = {
    tabs: [],           // 所有打开的标签页，每个 = { id(路径), name, content, originalContent }
    activeTabId: null,  // 当前激活的标签 id（就是文件路径）
    viewMode: 'edit',   // 当前视图：'edit' 编辑 / 'preview' 预览
    // ...
}

// 关键技巧：计算属性。currentFile 不是存出来的，是查出来的。
Object.defineProperty(state, 'currentFile', {
    get() {
        return state.tabs.find(t => t.id === state.activeTabId) || null
    }
})
```

**为什么用 `Object.defineProperty` + `get`？** 因为标签会切换。如果不做计算属性，每次切换标签都要手动同步 `state.currentFile = 新tab`，很容易漏。现在只改 `activeTabId`，所有读 `state.currentFile` 的地方自动拿到新值——**单一数据源，没有第二个真相**。

**`tab.id` 为什么直接用文件路径？** 路径天然唯一，不需要额外生成 id。同一个文件不会开两个 tab（打开前先 `tabs.find(t => t.id === path)` 检查，已开则只切换）。

### 9.2 多标签页：`renderTabs` / `activateTab` / `closeTab`

```
打开文件 openFileByPath
  → 已在 tabs 里？→ activateTab(已有 id)      （不重复开）
  → 不在？→ tabs.push({...}) → renderTabs()   （新增标签）
  → renderActiveFile() 渲染编辑器
```

- `renderTabs()`：遍历 `tabs` 生成一排 `.tab` 元素。文件名用 `textContent` 放（防 XSS）。未保存的标签显示 **橙色 ●**（`isDirty(t)` 为真就加 `.dirty` 类）
- `closeTab(tab)`：**关之前先问**——`isDirty(tab)` 时 `confirm('有未保存修改，确定关闭？')`。关闭后激活"邻居"标签：优先右边的，没有就左边，避免关掉后眼前一片空白
- **中键（鼠标滚轮键）关闭**：`mousedown` 事件里判断 `e.button === 1`

**未保存检测**还是老配方：`content !== originalContent`。每个 tab 各存一份"磁盘基线"，互不干扰。

### 9.3 MD 富文本渲染（这轮最核心的新能力）

之前 `.md` 文件被当纯文本塞进 textarea，全是 `#` 和 `>`。现在它有自己的解析管线：

```
MarkdownParser.parse(md文本) ──→ { html, languages }
        │                              │
        │                              └─ 有哪些代码块语言（供高亮用）
        ▼
buildPreviewHtml() 把 html 塞进预览面板
        ▼
highlightPreviewBlocks() 逐个找到 <code data-lang> 块，重新走语法高亮
```

**`MarkdownParser.parse` 的三大设计：**

1. **escape-first（先转义再格式化）**：先把所有原始文本里的 `<`、`>`、`&` 转成实体，再去匹配标题/列表/加粗等语法。这样用户 MD 里写的 `<script>` 永远只是文本，**从源头掐死 XSS**
2. **占位符保护**：内联代码 `` `code` ``、链接、图片里的特殊字符会被 ` ` 占位符"藏"起来，格式化完再还原，避免它们被误当成语法
3. **危险协议降级**：`javascript:` / `vbscript:` / `data:` 开头的链接直接降级成纯文本，不生成 `<a>`——防止点一下跑脚本

**`highlightPreviewBlocks` 为什么用 textContent 往返？** 解析器已经输出 HTML 了，高亮器只能在纯文本上工作。所以先取到 `<code>` 的 `textContent`（纯文本），高亮成带 `<span class="tok-xxx">` 的 HTML，再放回去。**先取文本再放回 → 高亮结果不会再被当成 HTML 执行**，双保险。

### 9.4 代码语法高亮：`SyntaxHighlighter.highlight`

`renderer/highlighter.js` 自己手写了一个极简 tokenizer（词法分析器），不用引入几百 KB 的 highlight.js：

- **master regex**：把关键字/字符串/数字/注释/函数/标签/属性等写成一条"优先级排列"的正则分支，从左到右贪婪匹配，每个片段分类
- **HTML 单独用状态机**：`<` 之后是标签名、属性、引号内容，状态在标签内/外之间切换
- 支持：js/ts、html/xml/svg、css、json、python、sh。**明确不支持 md** → `SyntaxHighlighter.supports('md') === false`，返回转义后的纯文本

**支持的语言要查表**：`app.js` 里 `langFromExt` 把 `.js/.ts/.html/.py` 等扩展名映射成语言名，高亮前先问 `supports(lang)`，不认识就原样显示。

### 9.5 编辑 / 预览一键切换

MD 文件打开时，编辑器头部出现「👁 预览」按钮（非 MD 文件按钮隐藏，因为没必要）：

```js
els.viewModeBtn.addEventListener('click', () => {
    // 按钮不是只管显示，它同时是"开关"：点击后翻到另一面
    state.viewMode = state.viewMode === 'preview' ? 'edit' : 'preview'
    if (state.viewMode === 'preview') showPreview()   // 切预览 → 渲染 HTML
    else renderEditor()                                // 切回编辑 → 重新显示 textarea
    // 按钮文字跟着变：预览时显示"✏️ 编辑"，编辑时显示"👁 预览"
})
```

两个面板 `#editor`（textarea）和 `#previewPane` 通过 `.hidden` 类切换显示。**预览是"只读快照"**：打开预览时重新解析当前内容，所以编辑后切到预览会看到最新效果。

### 9.6 文件管理：右键菜单搞定 6 种操作

空白处右键 → 菜单（新建文件/新建文件夹/刷新）；文件上右键 → 菜单（打开/重命名/复制/删除/在文件夹中显示）。

- `showContextMenu(x, y, items)`：动态生成 `.contextMenu` 元素，`position: fixed` 放在鼠标位置。**菜单是"生成出来用完销毁"**，不是常驻 6 个按钮
- `validName(name)`：非法字符（`\/:*?"<>|`）和空名直接 `alert` 拒绝，**改名前先校验**
- `createNewFile(dir)` / `createNewFolder(dir)`：`prompt('输入文件名')` → `validName` → 调 IPC `createFile`/`mkdir` → `reRenderChildren` 局部刷新（不用整棵树重画）
- `renameItem(item)`：`prompt` 拿新名 → IPC `rename`
- `deleteItem(item)`：**`confirm` 二次确认** → IPC `remove`（主进程 `fs.rm` 带 `recursive:true`，文件夹也能删）→ 如果删除的是当前打开的 tab，同时关掉
- `copyItem(item)`：**自动避名冲突**——已存在同名时循环追加" - 副本"，`a.txt` → `a - 副本.txt` → `a - 副本 2.txt`，绝不覆盖原文件

### 9.7 搜索 + 快速切换器（Ctrl+P）

**主进程** `fs:search`：从工作区根目录递归搜索文件名（`toLowerCase().includes`），**上限 200 条**防卡死，跳过 `.` 开头和 `node_modules`（噪音大），无权限目录静默跳过。

**渲染进程** `openQuickSwitcher`：弹出浮层，输入即搜（防抖 `debounce`，停止输入 150ms 才真的发请求，避免每个字符都打满磁盘）。

- `renderQsResults(results)`：**简单计分排序**——名字以关键词开头的排最前，包含的其次，且匹配到的文件优先于文件夹。体验接近 VS Code 的 Ctrl+P
- `moveQsIndex(delta)`：↑↓ 移动高亮，**索引越界取模回绕**（`(i + len) % len`），到底自动跳回顶
- `openQsSelection()`：Enter 打开选中项 → **`revealAndExpand(dirPath)`** 沿着父级逐层展开目录树并高亮该行，然后 `openFileByPath` 打开

`revealAndExpand` 是目录树"从结果回到位置"的关键：搜索结果是文件，但目录树是懒加载的（没展开过就没有 DOM 行）。它从根一路 `readDir` + 展开，把路径上每一层都渲染出来，最后才能定位到那一行。

### 9.8 暗色 / 亮色主题：改 CSS 变量，不动任何组件

这是换肤最优雅的方式：

```css
:root {
    --accent: #10b981;
    --bg-surface: #ffffff;
    --text: #26303e;
    /* ...几十个变量，所有组件都引用它们 */
}
:root[data-theme="dark"] {
    --accent: #34d399;
    --bg-surface: #111827;
    --text: #e5e7eb;
    /* ...只换变量的值 */
}
```

**任何组件都不用改**——只要它用的颜色来自变量。`toggleTheme()` 只做一件事：给 `<html>` 加/删 `data-theme="dark"`，CSS 选择器自动切换整套变量。**主题偏好存 `localStorage`**，下次启动 `applyTheme()` 读回，不丢失。

### 9.9 目录树增强

- `fileIconFor(name)`：按扩展名返回不同颜色的内联 SVG（图片紫、MD 橙、代码蓝、配置灰…），文件夹统一橙色——比清一色图标信息量大得多
- `expandAll()`：一键展开全部，**上限 300 个节点**防止几千个文件夹把树撑爆（这是"保护性上限"，宁可少展开也不能卡死）
- `collapseAll()`：清空所有子层，只留根目录第一层
- `showTooltipAt`：鼠标悬停树行超过 250ms → 顶部显示完整路径浮层（`position: fixed`），移开或滚动即隐藏
- `syncTreeSelection`：打开文件时用 `CSS.escape(path)` 做选择器转义后高亮对应行，保证标签和树始终同步

### 9.10 底部状态栏

三个 span，实时反映状态：

- `#statusLeft`：左侧「就绪」/「正在保存…」/「已保存」
- `#statusCenter`：当前文件路径
- `#statusRight`：**`updateCursorPos()`** 在 textarea 的 `input`/`click`/`keyup` 时重算——`selectionStart` 往前数 `\n` 得行号，再减行首得列号，显示"行 x · 列 y · UTF-8"

`updateCursorPos` 的关键：`value.slice(0, selectionStart).split('\n')`——把光标前的文本按换行切开，数组长度就是当前行号，最后一段的长度就是列号。**一行代码拿到光标位置**，是 textarea 场景的经典技巧。

### 9.11 本轮顺带发现的坑

1. **index.html 全量覆盖写不进去**：Write 工具对该文件全量重写报告"成功"但磁盘不变。绕过方案：先把新内容写到 `index.new.html`，`mv -f` 覆盖。之后所有改 `index.html` 都用"暂存 + mv"。
2. **命令行长度限制**：65KB 的 heredoc 一次性写入报 `ENAMETOOLONG`（Windows 命令行约 32KB 上限）。大内容必须拆文件写，不能塞进 bash 命令。

---

## 十、更新后的测试清单

```bash
npm start
```

1. 打开文件夹后，点左侧导航「全部展开 / 全部收起」看目录树行为
2. 双击两个不同文件 → 顶部出现两个标签，未保存的标签有橙色 ●
3. 中键点标签 → 关闭；关有未保存修改的标签 → 弹确认
4. 打开 `.md` 文件 → 点「👁 预览」→ 标题/引用/表格/代码块全部渲染，代码块带颜色高亮
5. 编辑 MD → 切预览 → 看到最新渲染效果
6. 点 `Ctrl+P` → 输入关键字 → ↑↓ 选择 → Enter 打开，目录树自动展开定位
7. 在文件上右键 → 重命名 / 复制（同名自动变" - 副本"）/ 删除（弹确认）
8. 点侧栏 🌙 → 全局切暗色，重启后主题保持
9. 底部状态栏：光标移动时"行 x · 列 y"实时更新
10. 顶部工具栏最左侧 ➕ 新建笔记 → 弹自研输入框 → 输名字确定 → 自动补 `.md` 并直接打开编辑
11. 排序按钮左边 📁 新建文件夹、最左边 📝 新建文件 → 同样弹自研输入框
12. 排序按钮右边 📂 切换工作区 → 弹出系统文件夹选择框 → 选新文件夹后整棵目录树重建、标签清空
13. 右键空白处 → 新建文件/新建文件夹 → 弹自研输入框，取消不创建

---

## 十一、自研模态框 + 切换工作区（本轮修复）

### 11.1 症状与根因：为什么"点了没反应"？

三个问题全是同一个根：**`frame: false` 的无边框窗口里，系统自带的 `window.prompt()` 弹不出来**。

```js
// 老代码长这样，看似没问题，实际 prompt 在无边框窗口里根本不显示
const name = window.prompt('新文件名：')   // 弹不出对话框，用户以为点了没反应
```

`prompt/confirm/alert` 是浏览器自带的"模态阻塞"对话框，Electron 无边框窗口对它的支持有缺陷——对话框不出现，代码却还在继续跑（或卡住），于是**"右键新建文件/文件夹没反应"**。

### 11.2 修复思路：覆盖全局对话框

不逐个改调用点（那样要改十几处），而是**把三个全局函数替换成自研 HTML 模态框**——所有老代码 `window.prompt(...)` 的写法自动走新模态框：

```js
window.prompt  = (message, defaultValue) => openModal({ title: message, showInput: true, defaultValue })
window.confirm = (message) =>               openModal({ title: '确认', message, showCancel: true })
window.alert   = (message) =>               openModal({ title: '提示', message, showCancel: false, okText: '知道了' })
```

**学习点：面向接口替换（Interceptor Pattern）。** 调用方只依赖 `window.prompt` 这个名字（接口），不关心实现是系统对话框还是 HTML 模态框——替换实现、零改动调用方，这就是"依赖抽象不依赖实现"。

### 11.3 踩坑：异步化之后必须 `await`

**这是本轮真正的 bug。** `openModal` 要等用户点按钮，所以返回 Promise（异步）；而老代码还按"同步返回字符串"来用：

```js
const name = window.prompt('笔记名称（自动补 .md）：')   // ← 现在返回 Promise！
if (!name) return                                        // Promise 是 truthy，永远继续
if (!validName(name)) { alert('名称包含非法字符'); return } // typeof Promise !== 'string' → 必弹警告！
```

`validName` 要求 `typeof name === 'string'`，传进去一个 Promise 对象必然失败——于是**同步**弹出「名称包含非法字符」，用户根本来不及输入。离屏测试（T1）立刻暴露：点新建笔记后模态框内容是"警告"而不是"输入框"。

修复 = 所有调用点补 `await`：

```js
const name = await window.prompt('笔记名称（自动补 .md）：')
const ok   = await window.confirm('确定删除…吗？')
```

改动涉及 6 处：`createNewNote` / `createNewFile` / `createNewFolder` / `renameItem` / `closeTab` / 退出确认。**核心教训：把同步 API 换成异步 API 时，调用方一个都不能漏，否则静默出诡异的错。**

### 11.4 自研模态框实现要点

- `openModal(opts)`：设置标题/正文/输入框，移除 `.hidden` 显示，`focus()+select()` 让输入框立即就绪；返回 Promise，把 `resolve` 存进 `modal.resolve`
- `closeModal(value)`：隐藏 + 把存好的 `resolve(value)` 调出来——**先隐藏再 resolve**，保证拿到值时模态框已关闭
- `Enter` 确定 / `Escape` 取消 / 点遮罩空白处取消——三个交互都映射到 `closeModal`
- 覆盖用 `document.getElementById` 而非 `$` 简写，因为模态框代码插入位置靠前、`$` 定义在后面（**代码执行顺序的坑**）

### 11.5 切换工作区（新功能）

之前进项目后只能用一个文件夹，加了个入口：排序按钮右边的 📂 按钮。

```js
async function switchWorkspace() {
    const res = await window.electronAPI.selectFolder()   // 复用启动时的选文件夹对话框
    if (!res.ok) return
    if (res.path === state.rootPath) return               // 选同一个 → 无事发生
    state.rootPath = res.path
    state.tabs = []; state.activeTabId = null; state.expanded.clear()  // 清空标签/展开状态
    state.history = []; state.historyIndex = -1
    updateNavBtns(); renderTabs(); await loadRoot()        // 整棵目录树重建
    recordRecentFolder(res.path); renderRecentFolders()   // 记入最近文件夹
}
```

**学习点：`selectFolder` 一个 IPC 处理器被两处复用**（启动选工作区 + 切换工作区）——能力只写一次，入口可以很多，这就是复用。

---

## 十二、第四轮：实用性 + 高级感四件套

这一轮做了 4 个功能：**自动会话恢复 + 最近文件、文件夹收藏、全文内容搜索、Markdown 增强**。全部沿用现有纯 JS 栈，没加任何新依赖。

### 12.1 自动会话恢复 + 真光标还原 + 最近文件

**自动恢复**：启动时 `init()` 末尾不再停在启动界面，而是**如果上次有会话就自动 `restoreSession()`**，直接回到上次的工作区 / 标签 / 光标。手动"恢复会话"按钮保留（没有会话时自动隐藏）。

**真光标还原**——这是"A 单元最容易糊弄过去、也最容易出 bug"的地方。之前只在状态栏显示"行 x · 列 y"，但光标没真正回到 textarea 那个位置。还原要两步：

```js
// ① 把"第 N 行"换算成"字符串里的字符偏移"
const lines = value.split('\n')
let offset = 0
for (let i = 0; i < n - 1; i++) offset += lines[i].length + 1  // 每行长度 + 1 个换行符
// ② setSelectionRange 定位 + scrollTop 粗滚动
editor.setSelectionRange(offset, offset)
editor.scrollTop = Math.max(0, (n - 4) * 21)  // 行高约 21px，让目标行别沉到最底下
```

**学习点：行号 ≠ 字符位置。** textarea 的 `selectionStart` 是**字符偏移**，不是行号。想定位到第 N 行，必须把前 N-1 行的长度（各 +1 换行符）累加起来。这个换算函数被抽成 `revealLine(lineNum)`，后面 C 单元"跳到内容命中行"也复用它——**一个工具函数多处复用**。

**最近文件**：`recordRecentFile` 之前只记录不展示（存在 `localStorage` 的 `emerald-recent-files`，最多 20 条）。这轮补了 `getRecentFiles()` + `renderRecentFiles()`，渲染到侧栏「🕘 最近文件」块，点击 `openFileByPath` 直接打开。

### 12.2 文件夹收藏

**改动前**：收藏只对文件有效（`toggleFavorite` 有 `if (!item.isDir)` 守卫），存的是**纯路径数组** `string[]`。

**改动后**：数据结构升级成对象数组，并且**兼容旧数据**：

```js
// 旧数据是 string[]，读到字符串就自动迁移成对象（isDir 未知按文件处理）
function getFavorites() {
    const raw = JSON.parse(localStorage.getItem(FAV_KEY) || '[]')
    return raw.map((f) => typeof f === 'string'
        ? { path: f, name: basename(f), isDir: false }
        : f)
}
```

**学习点 1：升级存储格式要写"迁移层"。** 用户机器上可能还存着旧格式，直接读新格式会崩。读取时判断"是字符串还是对象"再归一化，旧数据无缝升级。

**学习点 2：渲染区分类型。** `renderFavorites` 里文件夹显示 📁 图标，点击**进入该目录**（复用 `switchWorkspace` 的核心逻辑）；文件仍 `openFileByPath`。为此把"切到另一个目录"抽成 `enterDirectory(path)` 函数，切换按钮、收藏夹、后续功能三处复用。

### 12.3 全文内容搜索（Ctrl+P 升级）

之前 `Ctrl+P` 只搜**文件名**。这轮主进程新增 `fs:searchContent`：递归读文本文件、逐行匹配，返回命中行。

**保护性限制**（每条都对应一个真实风险）：
- 最多遍历 2000 个文件 → 大工作区不会卡死
- 单文件 ≤ 512KB → 跳过巨型文件
- 含 ` ` 空字符视为二进制跳过 → 不会把图片当文本读出来乱码
- 最多 50 个文件、每文件最多 5 条命中 → 结果面板不爆炸

**前端 `runSearch` 并行发两个请求**：

```js
const [nameRes, contentRes] = await Promise.all([
    window.electronAPI.search(state.rootPath, q),        // 文件名命中
    window.electronAPI.searchContent(state.rootPath, q), // 内容命中
])
```

**学习点：`Promise.all` 让两个独立 IPC 同时飞。** 顺序 await 会慢一倍，并行互不等待。

`renderQsResults` 把结果分两组渲染，中间加分隔线「文件名匹配」「内容匹配」；内容命中项额外显示**命中行预览**（关键字用 `<mark class="qs-mark">` 高亮）+ 「第 N 行」。点击内容命中项 → `openFileAndRevealLine(path, lineNum)`：

```js
await openFileByPath(path)          // 打开文件
if (state.viewMode !== 'edit') toggleViewMode()  // 预览态先切回编辑态，光标才有意义
revealLine(lineNum)                 // 复用 12.1 的定位函数跳到那一行
```

**高亮防注入**：`highlightText` 先 `escapeHtml` 再把关键字包 `<mark>`，文件内容里的 `<script>` 永远只是文本。

### 12.4 Markdown 增强：任务列表 + TOC + 双向链接 + 反链

四个功能，parser 和前端各出一半力。

**① 任务列表 `- [ ]` / `- [x]`（可勾选回写）**

parser（`md-parser.js`）在 `parseList` 里识别 `[ ]` / `[x]`，给条目打 `task` 标记并记下**源码行号** `lineNo`；`buildList` 渲染成 `<input type="checkbox" data-line="N">`。前端在 `previewContent` 上用**事件委托**监听 checkbox 的 `change`：

```js
// 根据 data-line 找到源文件那一行，把 [ ] 换成 [x]（或反向）
lines[lineNo - 1] = lines[lineNo - 1].replace(/\[([ xX])\]/i, cb.checked ? '[x]' : '[ ]')
// 写回磁盘 + 同步 textarea + 刷新"未保存"基线
f.originalContent = content
```

**学习点：`data-line` 是"预览 DOM ↔ 源文件"的桥梁。** 解析器输出时把行号写进 `data-*` 属性，前端拿到勾选事件就能精确定位源文件那一行，不用重新扫描整个文件。这就是 Obsidian 勾任务直接改源文件的效果。

**② TOC 目录（纯渲染层，不动 parser）**

`buildPreviewHtml` 生成 HTML 后，用临时 `div` 载入 DOM，遍历 `h1~h3`：给每个标题补 `id="toc-N"`，收集文本生成目录块插到正文前。**标题 ≥ 2 个才显示**（标题太少的文档不需要目录）。点击目录项 → `scrollIntoView({ behavior: 'smooth' })` 平滑滚动到锚点。

**学习点：临时 DOM 当"解析器"用。** 想操作"解析结果"但不想要现成 API 时，`div.innerHTML = html` 再 `querySelectorAll`，拿完结果即可。TOC 不影响源文件，属于"锦上添花的渲染增强"。

**③ 双向链接 `[[目标]]` / `[[目标|别名]]`**

parser 在 `renderInlineEscaped` 的链接处理**之后**加 wikilink 正则（顺序讲究：wikilink 不含 `](url)`，不会被普通链接正则吞掉）：

```js
text.replace(/\[\[([^\]|]+)(?:\|([^\]]+))?\]\]/g, (m, target, alias) => {
    const shown = alias || target          // 有别名显示别名，没有显示原名
    return token('<a class="wikilink" data-target="' + escapeHtml(target.trim()) + '">'
        + renderInlineEscaped(shown.trim()) + '</a>')
})
```

前端点击 `.wikilink` → `search(rootPath, 目标去扩展名)` 找同名文件，**优先 `.md`**，找到就 `openFileByPath` 打开，找不到 `alert` 提示。

**④ 反链面板**

编辑器头部新增「🔗 反链」按钮（仅 `.md` 文件显示，`updateViewModeBtn` 里判断）。点击 → `searchContent(rootPath, '[[' + 当前文件名)`——**复用全文搜索**，搜"谁在 `[[` 里引用了本文档名"，过滤掉本文档自己，列出引用方 + 命中行文本，点击打开。

**学习点：反链不用新 IPC，复用现有能力。** 找"谁引用了我"本质上就是"全文搜索某个字符串"，C 单元的 `searchContent` 直接拿来用。**新功能 = 已有能力的组合，而不是新造轮子。**

**D 单元的 3 个前端交互全部用事件委托**（`previewContent` 上挂 `change` + `click` 两个监听）：
- checkbox `change` → 任务回写
- `.wikilink` `click` → 跳转
- `.toc a` `click` → 平滑滚动

**学习点：事件委托 = 只管一个容器，子孙随便加。** 预览是"每次重新生成 innerHTML"，如果每个 checkbox/链接都单独 `addEventListener`，重新渲染就全丢了；委托给容器，重新生成内容后监听依然有效。

### 12.5 本轮测试清单

```bash
npm start
```

1. 打开工作区、编辑一个文件停在中间 → 重启应用 → 自动回到上次的工作区 + 标签 + 光标位置
2. 侧栏出现「🕘 最近文件」，点一个直接打开
3. 文件上右键 → 收藏；文件夹上右键 → 收藏；侧栏收藏区文件夹显示 📁 图标，点击进入该目录
4. `Ctrl+P` 搜一个**只出现在文件内容里**的词 → 出现「内容匹配」组 + 命中行预览 + 行号 → 点击跳到那一行
5. 打开含 `- [ ]` 的 MD → 预览 → 勾选任务 → 切回编辑看源文件变成 `- [x]`
6. MD 含 2 个以上标题 → 预览顶部出现目录，点击平滑滚动
7. MD 里写 `[[另一个笔记]]` → 预览显示为虚线链接 → 点击打开目标；写不存在的目标 → 弹提示
8. 打开笔记 A（被 B 引用）→ 点「🔗 反链」→ 列出 B 及引用行，点击打开 B
